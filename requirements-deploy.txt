# Prediction-only dependencies for the hosted Docker image (see Dockerfile).
# No PyTorch/TensorFlow: predict_onnx.py runs pokemon_model.onnx with
# onnxruntime, which fits in a free host's 512 MB of RAM.
onnxruntime
numpy
pillow
