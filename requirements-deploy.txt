# Prediction-only dependencies for the hosted Docker image (see Dockerfile).
# No TensorFlow: predictions use pokemon_model.tflite with the TFLite runtime
# (ai-edge-litert), which fits in a free host's 512 MB of RAM.
ai-edge-litert
numpy
pillow
requests
